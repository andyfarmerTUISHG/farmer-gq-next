# Sanity Schema

Create or modify Sanity schema following project patterns:

- Place in `/sanity/schemas/`
- Use TypeScript
- Include proper validation
- Add helpful descriptions
- Remember to run `npm run build && npm run deploy` after changes
